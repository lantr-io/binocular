target
files
.history
/.bsp
.bloop/
/.idea
/.idea_modules
/.classpath
/.project
/.settings
.scala-build
*.plutus
scala-cli.json
.DS_Store
.metals
.vscode
.direnv
metals.sbt
cardano-cli
cardano-address
.env
.contract-debug/
application.conf
/.bitcoin-regtest

# mill
/out/
docs/forktree-spec-v2.md
application-*.conf
# local, per-environment bridge/oracle configs (reference .env secrets) — never commit
preprod-*.conf
mermaid-filter.err
docs/superpowers/

# diagnostic-only: verbose-trace peg_out blueprint (regenerate via aiken build --trace-level verbose)
peg-out-debug-blueprint.json
